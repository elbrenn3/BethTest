  
name: schedule

on:
  schedule:
    # the 21st and 47th minute of every business hour (14-22 UTC, 9-17 EST) every day of the montch except the 15th and 31st, not on weekends
    - cron:  '21,47 14-22 0-14,16-30 * 1-5'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: Run a one-line script
      run: echo important business stuff!
